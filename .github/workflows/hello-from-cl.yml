name: Hello from Cl

on:
  push:
    branches:
      - copilot/hello-from-cl

jobs:
  hello:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Print message
        run: echo "Hello form Cl"
